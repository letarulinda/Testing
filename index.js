function ("name") {
  return name;
}
module.exports = name;
const name = require('./name');

test('string is at least character long and not long than 10 characters', () => {
  expect Throws error if the condition is not met;
});
function reverse ("name") {
  return reverse;
}
